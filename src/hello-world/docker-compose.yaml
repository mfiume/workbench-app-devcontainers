services:
  app:
    # The container name must be "application-server"
    container_name: "application-server"
    # Use pre-built Python image
    image: "python:3.11-slim"
    environment:
      ENVIRONMENT: "Verily Workbench"
    # Command: install Flask and run the app
    command: >
      bash -c "pip install --no-cache-dir flask werkzeug &&
               cd /workspace &&
               python app.py"
    restart: always
    volumes:
      - .:/workspace:cached
      - work:/home/jovyan/work
    # The port specified here will be forwarded and accessible from the
    # Workbench UI.
    ports:
      - 8888:8888
    # The service must be connected to the "app-network" Docker network
    networks:
      - app-network
    # SYS_ADMIN and fuse are required to mount workspace resources into the
    # container.
    cap_add:
      - SYS_ADMIN
    devices:
      - /dev/fuse
    security_opt:
      - apparmor:unconfined

volumes:
  work:

networks:
  # The Docker network must be named "app-network". This is an external network
  # that is created outside of this docker-compose file.
  app-network:
    external: true
